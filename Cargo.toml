[package]
name = "rust_q_sim"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
petgraph = "0.6.0"
serde = { verion = "1.0.137", features = ["derive"] }
quick-xml = { version = "0.23.0", features = ["serialize"] }
